version: '3'
services:
  sinatra:
    build:
      context: ./sinatra
    ports:
      - "8080:8080"
    environment:
      - PGHOST=192.168.100.166
      - PGDATABASE=oag
      - PGUSER=jakub
      - PGPASSWORD=password
      - PORT=8080
      - APP_ENV=production
      - COUNTERHOST=counter
      - COUNTERPORT=8080
  counter:
    build:
      context: ./counter
    environment:
      - PORT=8080
      - APP_ENV=production
