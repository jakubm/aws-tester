version: '3'
services:
  sinatra:
    build:
      context: ./sinatra
    ports:
      - "8080:8080"
    environment:
      - PGHOST=192.168.100.166
      - PGDATABASE=oag
      - PGUSER=jakub
      - PGPASSWORD=password
      - PORT=8080
      - APP_ENV=production
